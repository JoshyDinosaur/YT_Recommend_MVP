————————————————————————————————Stateful Widget————————————————————————————————StatefulWidget	architecture where the logic is separated into distinct classes (State: handles immutable configuration of the UI, Widget: builds/rebuilds the changes.		Widget Class (class)— immutable class, only job is to provide the createState() method	State Class (_class)— mutable class, persists even when the tree rebuilds, where your data livescreateState()	keep the data you want to maintain through Widget rebuilds	ensures there is a separated ‘brain’ for the widget that can keep track of changes over timeState<T>	-class that is the persistent logic and data layer of a stateful widget	-essientially the ‘memory’ of a stateful widget, where the “real work” happens	-three primary elements: Logic, Data, the Build Context	-Provides the setState() method		primary mechanism in Flutter notifying the Framework that the internal state of an object has changed		triggers a rebuildsetState()	-in Flutters declarative UI, you don’t manipulate UI elements (like, changing a labels text)	-instead, you change the data, and setState() tells flutter to re-run the build() method to reflect those changes	-only triggers a rebuild for the widget it belongs to and its children, not the whole app————————————————————————————————OAuth 2.0————————————————————————————————OAuth 2.0 Scope	-a mechanism used to limit the access to a users account	-instead of giving full access, allow you to request access to only SPECIFIC ACTIONS or DATA your app needs	-these permissions are formatted as URLs, so the provider can own a namespace for naming them		-youtubeapi.youtubereadonlyscopeGoogleSignInAccount	-the object returned when a user successfully signs it, acts ass the “Identity Card” for the user within my appGoogleSignIn	-the class that serves as the central manager of the authentication flow	-method that returns a GoogleSignInAccount object, w/ email, id and the authentication method to get user auth tokenAuthentication / Authorization-Google has separated the two, and now the logging in (Authentication) is separate from the permissions (Authorization)	-initialize() <— sets up the “Who are you?” Connection (Authent.)	-authentication() <— sets up the “What can you do.” Connection (Authoriz.)————————————————————————————————Stream Subscription————————————————————————————————Stream<T>	-(Source) a ‘radio station’, broadcasting data over time	-asynchronos sequence of data, arriving at unpredictable times		.listen()		-when you call this method on a Stream, it returns a StreamSubScription object	-in Dart/Flutter, many objects expose Streams to handle data that changes over time, any property that begins with/ on		-it’s a strong signal, begins with on, returns a Stream<T>StreamSubscription	-(Receiver) this object is the “remote control” for the specific data connection	-will use to manage the flow of data from the Stream (.pause(), .resume(), .cancel()DataHandler, Callback	-the .listen() method accepts a DataHandler (specific callback function) that passes in the data that may change, and the function to execute